## Implementation: LTV utility + display handling
- Pattern used: extracted computeLtv/computeHealthFactor utilities and reused in page + BorrowPosition.
- Challenges: ensuring missing prices return null (no raw-amount fallback) and UI shows "--".
- Solution: centralized compute functions and conditional formatting in UI.
- Tests added: frontend/__tests__/utils/position.test.ts
- Commit: fb323c7

## PR Review: PR #113
- Requested changes: remove raw-amount fallback, extract LTV logic, add tests for null/zero cases.
- Learning: avoid silent fallbacks when data is missing; centralize core calculations.
- Convention clarified: add utility functions in frontend/lib/utils and add unit tests with vitest.
